{% trans_default_domain 'about' %}
<section class="mb-5">
    <div class="mgt-section-heading"><h2>{{ 'organizers.title'|trans }}</h2></div>
    <div class="card">
        <div class="card-body">
            <p class="mb-0">{{ 'organizers.generic'|trans }}</p>
        </div>
    </div>
</section>
