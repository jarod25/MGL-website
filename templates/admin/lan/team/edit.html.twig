{% extends 'base.html.twig' %}{% block title %}{{ 'admin.team.edit_title'|trans({ '%name%': team.name }) }}{% endblock %}{% block body %}<div class="container py-4"><h1 class="mb-4">{{ 'admin.team.edit_title'|trans({ '%name%': team.name }) }}</h1>{{ form_start(form) }}{{ form_row(form.name) }}<div class="d-flex gap-2 mt-4"><button class="btn btn-primary" type="submit">{{ 'admin.team.save'|trans }}</button><a class="btn btn-outline-secondary" href="{{ path('app_admin_lan_team_show',{id:team.id}) }}">{{ 'admin.team.back_to_team'|trans }}</a></div>{{ form_end(form) }}</div>{% endblock %}
